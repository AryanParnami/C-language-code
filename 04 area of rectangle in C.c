#include <stdio.h>

int main()
{
//******************************** rectangle area **************************************    
    int length, breadth;
    printf("Whta is the length of the rectangle\n");
    scanf("%d", &length);

    printf("Whta is the breadth of the rectangle\n");
    scanf("%d", &breadth);
    
    printf("The area of the rectangle is %d",length*breadth);
   

    return 0;
}